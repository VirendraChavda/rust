Bootstrap extended Python-backend essential parity crates.

Task source:
- Read `.claude/task.md` and apply optional family filters from `Scope`.

Steps:
1. Run PowerShell: ./scripts/dev/bootstrap_python_backend_essentials.ps1
   or bash: bash scripts/dev/bootstrap_python_backend_essentials.sh
2. Report created and skipped modules.
3. Group implementation order by module family.
