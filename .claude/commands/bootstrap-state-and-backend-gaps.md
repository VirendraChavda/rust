Bootstrap remaining state-machine and backend gap crates.

Task source:
- Read `.claude/task.md` and apply optional family filters from `Scope`.

Steps:
1. Run PowerShell: ./scripts/dev/bootstrap_state_and_backend_gaps.ps1
   or bash: bash scripts/dev/bootstrap_state_and_backend_gaps.sh
2. Report created and skipped modules.
3. Group implementation order by module family.
