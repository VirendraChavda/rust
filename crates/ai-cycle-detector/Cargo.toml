[package]
name = "ai-cycle-detector"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Cycle detection and diagnostics"
repository = "https://github.com/VirendraChavda/rust"
readme = "README.md"
keywords = ["ai", "backend", "rust"]
categories = ["development-tools"]

[dependencies]
ai-graph-core = { path = "../ai-graph-core" }
ai-dag-planner = { path = "../ai-dag-planner" }
